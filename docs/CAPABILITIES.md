| Capability | Notes |
|------------|--------|
| Compose deck | `deck.yaml` + HTML partials → themed `dist/index.html` |
| Hand deck ops | serve / PDF / standalone for existing `index.html` |
| Theme | `theme/calliope.css` — house visual system |
| Outline | Skill `calliope-outline` → acts/slides in `deck.yaml` |
| Loop | Skill `calliope-loop` — brief → build → evidence |
