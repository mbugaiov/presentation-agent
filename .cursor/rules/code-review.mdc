---
description: Automated PR code review for the presentation-agent engine — policy for cursor-agent on GitHub PRs.
alwaysApply: false
---

# Calliope engine — automated code review

The GitHub PR workflow runs headless `cursor-agent` on every PR.

## Scope

- Diff vs base only. Engine files — not live `projects/<slug>/`.

## Block on

## MUST-HAVE — tests (Themis shared)

Behavior change (scripts/libs/skills/workflows/CLI/gates) without **new or updated**
tests → **Blocking**. Align with `themis-agent/review-rules/ (via build_review_prompt.sh)`
(CI also loads `.themis-agent/review-rules/ (via build_review_prompt.sh)`).


1. Live project data in git (except `projects/_template/` and `examples/`)
2. Product/customer leaks in engine docs/skills (outside `examples/`)
3. Secrets
4. **Missing or stale tests (MUST-HAVE)** — new/changed CLI behavior without **new or updated** coverage in `tests/run_tests.sh` / vitest (file-exists/`grep` smoke alone is not enough)
5. Portability (`/Users/...`, live customer URLs in skills/rules)
6. Forking a second visual system instead of `theme/calliope.css`
7. Implementing product app code inside this engine

## Isolation

Job **`isolation (Themis)`** is separate. Fold obvious leaks into Blocking.

## Output

- `## Summary`
- `## Blocking issues` — or `None.`
- `## Suggestions` — or `None.`
- `## High priority issues` — or `None.`

End LGTM only when Blocking is clean.
